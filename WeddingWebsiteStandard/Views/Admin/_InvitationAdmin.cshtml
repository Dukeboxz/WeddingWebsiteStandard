@model IEnumerable<WeddingWebsiteStandard.Models.InvitationModel>

@{
    ViewBag.Title = "_InvitationAdmin";
}

<h2>_InvitationAdmin</h2>

<p>
    @Html.ActionLink("Add Invitation", "Create")
</p>

<div id="InvitationList">
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.passCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.passCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
    </div>