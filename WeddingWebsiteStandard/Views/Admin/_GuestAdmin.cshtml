@model IEnumerable<WeddingWebsiteStandard.Models.Guest>

@{
    ViewBag.Title = "_GuestAdmin";
}

<h2>_GuestAdmin</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<div class="container">
    <div class="col-sm-5">
        <div class="list-group">
            @foreach (var item in Model)
            {
                <button class="list-group-item list-group-item-action active">@item.firstName + " " + @item.secondName </button>

            }
        </div>
    </div>

    <div class="col-sm-6"></div>

</div>



   
}

